import React from 'react'

const MessageList = (props) => {
  return (
    <div id="MessageList">
      Message List. Will show event that can be clicked on and display messages on the right from the users.
    </div>

  )
}

export default MessageList
